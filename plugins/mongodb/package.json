{
  "name": "@linkedmink/multilevel-aging-cache-mongodb",
  "version": "0.5.1",
  "description": "Package implements MongoDB for @linkedmink/multilevel-aging-cache",
  "keywords": [
    "cache",
    "memory",
    "redis",
    "mongodb",
    "database",
    "storage",
    "distributed"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "author": {
    "name": "Harlan Sang",
    "email": "harlan@linkedmink.net"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/LinkedMink/multilevel-aging-cache"
  },
  "scripts": {
    "build": "tsc --project ./tsconfig.json",
    "test": "jest",
    "lint": "eslint -c ../../.eslintrc.js --ext .ts src",
    "format": "prettier --config ../../.prettierrc.js --write src/**/*.ts tests/**/*.ts *.json",
    "prepublishOnly": "yarn build",
    "doc": "typedoc"
  },
  "peerDependencies": {
    "@linkedmink/multilevel-aging-cache": "workspace:*",
    "mongodb": "^4.0.0"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "^2.1.1",
    "mongodb": "^4.2.1"
  }
}
