{
  "name": "@linkedmink/multilevel-aging-cache-mongodb",
  "version": "0.4.9",
  "description": "Package implements MongoDB for multilevel-aging-cache",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "author": {
    "name": "Harlan Sang",
    "email": "harlan@linkedmink.space"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/LinkedMink/multilevel-aging-cache"
  },
  "scripts": {
    "build": "tsc --project ./tsconfig.json",
    "test": "jest",
    "lint": "eslint -c ../../.eslintrc.js --ext .ts src",
    "format": "prettier --write src/**/*.ts tests/**/*.ts *.js *.json",
    "link": "npm run build && npm link",
    "prepublishOnly": "npm run build",
    "doc": "typedoc"
  },
  "peerDependencies": {
    "@linkedmink/multilevel-aging-cache": "0.4.9",
    "mongodb": "3.x"
  }
}
